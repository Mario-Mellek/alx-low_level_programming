#ifndef MACRO_H
#define MACRO_H

/**
 * The ABS(x) function-like macro is a preprocessor
 * macro in C that computes the absolute value of a number x.
 * The absolute value of a number is its distance from zero.
 */

#define ABS(x) ((x) < 0 ? -(x) : (x))

#endif
