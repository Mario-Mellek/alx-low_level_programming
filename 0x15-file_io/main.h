#ifndef HEADER_H
#define HEADER_H
#include <stddef.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>

int _putchar(char c);
ssize_t read_textfile(const char *filename, size_t letters);

#endif
